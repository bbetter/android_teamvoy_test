<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">
    <LinearLayout android:id="@+id/reviewThumbnail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="false">
        <ImageView android:id="@+id/reviewImage"
            android:layout_height="121dp"
            android:layout_width="121dp"
            android:contentDescription="@string/app_name"
            />
    </LinearLayout>

    <TextView android:id="@+id/tvReviewAuthor"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:text=""
        android:layout_toRightOf="@+id/reviewThumbnail"/>
    <TextView android:id="@+id/tvReviewText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:text=""
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignBottom="@+id/reviewThumbnail"
        android:layout_below="@+id/tvReviewAuthor"
        android:layout_toRightOf="@+id/reviewThumbnail"
        android:layout_toEndOf="@+id/reviewThumbnail"
        android:ellipsize="end"
        android:lines="4"/>
</RelativeLayout>